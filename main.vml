/*
this is a multi-line comment
*/

// this is a comment

thing :: struct {
    a: i32;
    b: f32;
}

onion :: union {
    a: i32;
    b: f32;
}

fruit_kinds :: enum {
    banana;
    strawberry;
}

this_thing := 3;

main :: () -> i32 {
    a := '\a';
    //d, f := this_function();
    print("Hello world!");
}
